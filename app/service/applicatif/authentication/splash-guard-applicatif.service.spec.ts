/*
import { TestBed, inject } from '@angular/core/testing';

import { SplashGuardApplicatifService } from './splash-guard-applicatif.service';

describe('SplashGuardApplicatifService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SplashGuardApplicatifService]
    });
  });

  it('should ...', inject([SplashGuardApplicatifService], (service: SplashGuardApplicatifService) => {
    expect(service).toBeTruthy();
  }));
});
*/
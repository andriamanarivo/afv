/*
import { TestBed, inject } from '@angular/core/testing';

import { PseudoValidationService } from './pseudo-validation.service';

describe('PseudoValidationService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PseudoValidationService]
    });
  });

  it('should ...', inject([PseudoValidationService], (service: PseudoValidationService) => {
    expect(service).toBeTruthy();
  }));
});
*/

/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ListTermesInterditComponent } from './list-termes-interdit.component';

describe('ListTermesInterditComponent', () => {
  let component: ListTermesInterditComponent;
  let fixture: ComponentFixture<ListTermesInterditComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListTermesInterditComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ListTermesInterditComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/

/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PratiquePresentationComponent } from './pratique-presentation.component';

describe('PratiquePresentationComponent', () => {
  let component: PratiquePresentationComponent;
  let fixture: ComponentFixture<PratiquePresentationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PratiquePresentationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PratiquePresentationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
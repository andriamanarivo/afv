/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SituationPresentationComponent } from './situation-presentation.component';

describe('SituationPresentationComponent', () => {
  let component: SituationPresentationComponent;
  let fixture: ComponentFixture<SituationPresentationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SituationPresentationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SituationPresentationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RecherchePresentationComponent } from './recherche-presentation.component';

describe('RecherchePresentationComponent', () => {
  let component: RecherchePresentationComponent;
  let fixture: ComponentFixture<RecherchePresentationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RecherchePresentationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RecherchePresentationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
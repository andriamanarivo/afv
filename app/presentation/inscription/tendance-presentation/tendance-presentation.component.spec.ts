/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TendancePresentationComponent } from './tendance-presentation.component';

describe('TendancePresentationComponent', () => {
  let component: TendancePresentationComponent;
  let fixture: ComponentFixture<TendancePresentationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TendancePresentationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TendancePresentationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
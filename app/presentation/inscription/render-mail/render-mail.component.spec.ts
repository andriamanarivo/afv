/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RenderMailComponent } from './render-mail.component';

describe('RenderMailComponent', () => {
  let component: RenderMailComponent;
  let fixture: ComponentFixture<RenderMailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RenderMailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RenderMailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { OrientationPresentationComponent } from './orientation-presentation.component';

describe('OrientationPresentationComponent', () => {
  let component: OrientationPresentationComponent;
  let fixture: ComponentFixture<OrientationPresentationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ OrientationPresentationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(OrientationPresentationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { VousetesPresentationComponent } from './vousetes-presentation.component';

describe('VousetesPresentationComponent', () => {
  let component: VousetesPresentationComponent;
  let fixture: ComponentFixture<VousetesPresentationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ VousetesPresentationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(VousetesPresentationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/

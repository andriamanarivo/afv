/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ComptePresentationComponent } from './compte-presentation.component';

describe('ComptePresentationComponent', () => {
  let component: ComptePresentationComponent;
  let fixture: ComponentFixture<ComptePresentationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ComptePresentationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ComptePresentationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/

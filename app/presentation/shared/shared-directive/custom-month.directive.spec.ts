/*
import { CustomMonthDirective } from './custom-month.directive';

describe('CustomMonthDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomMonthDirective();
    expect(directive).toBeTruthy();
  });
});
*/

/*
import { CustomDayDirective } from './custom-day.directive';

describe('CustomDayDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDayDirective();
    expect(directive).toBeTruthy();
  });
});
*/
import { TwoColumnPipe } from './two-column.pipe';

describe('TwoColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new TwoColumnPipe();
    expect(pipe).toBeTruthy();
  });
});

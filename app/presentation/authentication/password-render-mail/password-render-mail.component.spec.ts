/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PasswordRenderMailComponent } from './password-render-mail.component';

describe('PasswordRenderMailComponent', () => {
  let component: PasswordRenderMailComponent;
  let fixture: ComponentFixture<PasswordRenderMailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PasswordRenderMailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PasswordRenderMailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
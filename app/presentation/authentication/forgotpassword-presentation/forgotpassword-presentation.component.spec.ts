/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ForgotpasswordPresentationComponent } from './forgotpassword-presentation.component';

describe('ForgotpasswordPresentationComponent', () => {
  let component: ForgotpasswordPresentationComponent;
  let fixture: ComponentFixture<ForgotpasswordPresentationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ForgotpasswordPresentationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ForgotpasswordPresentationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
